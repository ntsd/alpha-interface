name: AlphaInterfaceContract
description: AlphaInterfaceContract description
structs:
  crop:
    id: String // crop id `name_country`
    name: String // name of the crop
    country: String // the country name
    yield: Int64 // yield value of the crop unit hectogramme (100 grammes) per hectare
    updatedAt: Int64 // unix timestamp
  Position:
    crop: id // crop id
    price: Int64 // price when open the position
    amount: Int64 // amount to transfer in IOTA
    status: String // opening, closed
    owner: AgentID // agent id of the owner
typedefs: {}
state:
  owner: AgentID // current owner of this smart contract
  positions: Position[]
funcs:
  init:
    params:
      owner: AgentID? // optional owner of this smart contract
  setOwner:
    access: owner // current owner of this smart contract
    params:
      owner: AgentID // new owner of this smart contract
  transfer:
    params:
      number: Int64 // the number IOTAs to transfer
      receiver: AgentID // the receiver agent id
views:
  getOwner:
    results:
      owner: AgentID // current owner of this smart contract
  viewTransactions:
    results:
      transactions: Transaction[]
